<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("partials/head") %>
  <title>Forgot Password</title>
  <style>
    body {
      background: url("/images/ds-bg.png") no-repeat center center fixed;
      background-size: cover;
      font-family: 'Segoe UI', sans-serif;
      color: white;
    }

    .form-box {
      background-color: rgba(0, 0, 0, 0.75);
      padding: 30px;
      margin: 80px auto;
      width: 400px;
      border-radius: 10px;
      box-shadow: 0 0 10px black;
    }

    h2 {
      color: #f57c00;
      text-align: center;
    }

    label {
      display: block;
      margin: 15px 0 5px;
    }

    input[type="email"] {
      width: 100%;
      padding: 10px;
      border: none;
      border-radius: 5px;
    }

    button {
      background-color: #f57c00;
      color: white;
      border: none;
      width: 100%;
      padding: 10px;
      margin-top: 20px;
      border-radius: 5px;
      cursor: pointer;
    }

    .message {
      margin-top: 15px;
      text-align: center;
    }

    .message.success {
      color: #00e676;
    }

    .message.error {
      color: #ff5252;
    }
  </style>
</head>
<body>
  <div class="form-box">
    <h2>Forgot Password 🔐</h2>

    <form action="/auth/forgot" method="POST">
      <label for="email">Enter your registered email</label>
      <input type="email" name="email" id="email" required />
      <button type="submit">Send Reset Link</button>
    </form>

    <% if (message) { %>
      <p class="message success"><%= message %></p>
    <% } %>

    <% if (error) { %>
      <p class="message error"><%= error %></p>
    <% } %>
  </div>
</body>
</html>
